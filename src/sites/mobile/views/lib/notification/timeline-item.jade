li.notification(
		class= notification.type
		data-id= notification.id
		data-cursor= notification.cursor
		data-notification-type= notification.type)
	time(datetime=notification.createdAt, data-display-type='relative')= notification.createdAt
	div.main
		case notification.type
			when 'self-notification'
				p= notification.content.text
			when 'application'
				a.icon-anchor(href='/app/' + notification.app.screenName, title= notification.app.description, rel='icon')
					img.icon(src=notification.app.iconImageUrl, alt='icon')
				div.text
					p
						i.fa.fa-info-circle
						span= notification.content.text
			when 'install-app'
				a.icon-anchor(href='/app/' + notification.content.app.screenName, title= notification.content.app.description, rel='icon')
					img.icon(src=notification.content.app.iconImageUrl, alt='icon')
				div.text
					p
						i.fa.fa-plug
						| アプリがインストールされました:
						br
						a(href= '/app/' + notification.content.app.screenId)= notification.content.app.name
			when 'uninstall-app'
				a.icon-anchor(href='/app/' + notification.content.app.screenName, title= notification.content.app.description, rel='icon')
					img.icon(src=notification.content.app.iconImageUrl, alt='icon')
				div.text
					p
						i.fa.fa-ban
						| アプリがアンインストールされました:
						br
						a(href= '/app/' + notification.content.app.screenId)= notification.content.app.name
			when 'follow'
				a.icon-anchor(href='/' + notification.content.user.screenName, title= notification.content.user.comment != null ? notification.content.user.comment : '@' + notification.content.user.screenName, rel='icon')
					img.icon(src=notification.content.user.iconImageUrl, alt='icon')
				div.text
					p
						i.fa.fa-user-plus
						a(href= '/' + notification.content.user.screenName)= notification.content.user.name
						| さんにフォローされました。
			when 'status-repost'
				a.icon-anchor(href='/' + notification.content.user.screenName, title= notification.content.user.comment != null ? notification.content.user.comment : '@' + notification.content.user.screenName, rel='icon')
					img.icon(src=notification.content.user.iconImageUrl, alt='icon')
				div.text
					p
						i.fa.fa-retweet
						a(href= '/' + notification.content.user.screenName)= notification.content.user.name
						| さんが
						a.status-preview(href= '/' + me.screenName + '/status/' + notification.content.status.id)= notification.content.status.text
						| をRepostしました。
			when 'status-favorite'
				a.icon-anchor(href='/' + notification.content.user.screenName, title= notification.content.user.comment != null ? notification.content.user.comment : '@' + notification.content.user.screenName, rel='icon')
					img.icon(src=notification.content.user.iconImageUrl, alt='icon')
				div.text
					p
						i.fa.fa-star
						a(href= '/' + notification.content.user.screenName)= notification.content.user.name
						| さんが
						a.status-preview(href= '/' + me.screenName + '/status/' + notification.content.status.id)= notification.content.status.text
						| をふぁぼりました。
			when 'bbs-thread-post'
				a.icon-anchor(href='/' + notification.content.user.screenName, title= notification.content.user.comment != null ? notification.content.user.comment : '@' + notification.content.user.screenName, rel='icon')
					img.icon(src=notification.content.user.iconImageUrl, alt='icon')
				div.text
					p
						i.fa.fa-comment
						a(href= '/bbs/thread/' + notification.content.thread.id)= notification.content.thread.title
						| に投稿があります:
					a.post-preview(href= '/bbs/thread/' + notification.content.thread.id + '#post-' + notification.content.post.id)= notification.content.post.text
