extends ../../../../ui

block title
	| Webサイトの設定 - Misskey

block header
	i.fa.fa-cog
	| Webサイト

block content
	main(role='main')
		form#form
			input.ui-input-url(name='url', type='url', value= me.url)
			button(type='submit')
				| 更新
