extends ../main

block meta
	script!= 'var TALK_TYPE = "group";'
	script!= 'var GROUP = ' + JSON.stringify(group) + ';'

block title
	= 'トーク: ' + group.name

block header
	= 'トーク: ' + group.name

block empty-stream-message
	| このグループにまだメッセージはありません
