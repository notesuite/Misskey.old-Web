mixin content(post)
	include ../../../../text-parser.jade
	case post.type
		when 'status'
			div.text!= parse(post.text)
		when 'photo'
			div.text!= parse(post.text)
			ol.photos
				each photo in post.photos
					li.photo
						img.image(src= photo.url, alt= photo.name, title= photo.name)
