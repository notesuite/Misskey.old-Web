extends ../../../ui
include ../../../common/views/user-widget

block title
	= user.name + 'さんがフォローしているユーザー'

block meta
	meta(name='description', content= user.name + 'さんがフォローしているユーザー')
	meta(name='keywords', content='Misskey,' + user.screenName)

block content
	-
		function comma(str) {
			return String(str).replace( /(\d)(?=(\d\d\d)+(?!\d))/g, '$1,')
		}
	
	main(role='main')
		ol.users
			each followee in following
				li.user
					+user-widget(followee)
