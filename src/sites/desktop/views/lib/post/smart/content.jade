mixin content(post)
	case post.type
		when 'status'
			div.text!= post.text
		when 'photo'
			div.text!= post.text
			ol.photos
				each photo in post.photos
					li.photo
						img.image(src= photo.url, alt= '')
